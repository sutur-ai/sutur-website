'use client';
import {useState} from 'react';import SuturMark from '@/components/brand/SuturMark';
const links=[['Solutions','#solutions'],['Agents','#agents'],['Process','#process'],['Team','#team']];
export function Header(){const[open,setOpen]=useState(false);return <header className="site-header"><a className="brand" href="#top" aria-label="Sutur home"><SuturMark/></a><nav className="desktop-nav" aria-label="Primary">{links.map(([t,h])=><a key={h} href={h}>{t}</a>)}<a className="button small" href="#book">Book a call</a></nav><button className="menu" aria-label="Open menu" aria-expanded={open} onClick={()=>setOpen(true)}>Menu</button>{open&&<div className="mobile-nav" role="dialog" aria-modal="true" aria-label="Mobile navigation"><button aria-label="Close menu" onClick={()=>setOpen(false)}>Close</button><nav>{links.map(([t,h])=><a key={h} href={h} onClick={()=>setOpen(false)}>{t}</a>)}<a href="#book" onClick={()=>setOpen(false)}>Book a call</a></nav></div>}</header>}
