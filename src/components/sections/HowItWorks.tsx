import {processSteps} from '@/content/process';import SectionLabel from '@/components/ui/SectionLabel';export function HowItWorks(){return <section className="section" id="process"><SectionLabel>A clear route to launch</SectionLabel><h2>See the direction before the build begins.</h2><ol className="timeline">{processSteps.map(([title,text],i)=><li key={title}><span>{String(i+1).padStart(2,'0')}</span><h3>{title}</h3><p>{text}</p></li>)}</ol></section>}
