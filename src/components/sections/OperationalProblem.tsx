import {operationalIssues} from '@/content/services';import SectionLabel from '@/components/ui/SectionLabel';export function OperationalProblem(){return <section className="section problem" aria-labelledby="problem-title"><div><SectionLabel>The operational gap</SectionLabel><h2 id="problem-title">Growth gets harder when the business is scattered.</h2><p className="lead">A company should not need to reconstruct its own story across spreadsheets, messages, paper, and disconnected software.</p></div><ol className="issue-flow">{operationalIssues.map((x,i)=><li key={x}><span>{String(i+1).padStart(2,'0')}</span>{x}</li>)}</ol></section>}
